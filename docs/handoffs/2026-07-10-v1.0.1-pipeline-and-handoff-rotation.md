# Pre-v1.0.1 handoff rotation (2026-07-10)

> Archived: 2026-07-10 07:05 by trae
> Reason: HANDOFF.md grew to 283 lines, exceeding the 100-line cap from
> `D:\AI_Data\Shared\Protocols\MULTI_AGENT_PROJECT_PROTOCOL.md` §4.
> This file holds the Recent History that used to live in the root
> `HANDOFF.md`, plus the most recent substantive changes.

This archive is **read-only** by convention. Future updates go into
fresh `docs/handoffs/YYYY-MM-DD-<topic>.md` files; the root
`HANDOFF.md` only carries the active context.

---

## v1.0.1 — negative-keyword veto contract

- 2026-07-10 06:55 trae: implemented the `negative_keywords` /
  `negative_patterns` veto contract on `VocabularyEntry`. Outline
  bumped to v1.0.1; `typical-chickens-rabbits` now vetoes on
  geometry discriminators so the g8 fixture's 12.5 小结 stays on
  `geometry-plane-*` and `typical-chickens-rabbits` is correctly
  suppressed. xfail removed; replaced by
  `test_chicken_rabbit_vetoed_from_geometry_summary` (parametrized)
  and `test_chapter_summary_still_classified_as_geometry`. Added
  two synthetic veto tests plus a loader test for invalid regex in
  `negative_patterns`. Schema doc updated (required: keywords
  relaxed; new fields documented); outline README gains a
  "Negative keywords and patterns" subsection; new decision
  `2026-07-10-negative-keyword-veto.md`. Pushed to private remote
  as `b1d4c1d` on top of `0fd6523`. Full suite:
  **73 passed / 0 xfailed / 0 warnings**.

## Warning cleanup

- 2026-07-10 06:35 trae: zeroed out the two noisy pytest warnings.
  Removed `uni=True` from `src/pdf2dt/export/renderer.py:71-76`
  (deprecated in fpdf2 ≥ 2.5.1) and dropped the unused
  `pytest-asyncio` config + dev dependency from `pyproject.toml`
  (no test in the suite uses `@pytest.mark.asyncio`). Full suite:
  68 passed / 1 xfailed / **0 warnings**.

## Stage 4c + Stage 7 (Mode B) push

- 2026-07-10 06:08 trae: implemented Stage 4c export planner and
  Stage 7 PDF renderer for Mode B. New package
  `src/pdf2dt/export/{__init__,planner,renderer}.py` with
  `ExportPlanner`, `ExportPlan`, `ExportPlanCollection`,
  `PdfRenderer`, `plan_exports`, `render_exports`. CLI gained
  `--mode {A,B,C}` and `--export` (requires `--book-view`).
  `ExportPlanner` groups BookView items by leaf topic, sorts by
  Mode-B heuristics, and adds a `_misc` fallback.
  `PdfRenderer` uses `fpdf2` with local image embedding and Windows
  CJK font fallback (YaHei/SimHei → DejaVu placeholder). Added
  6 integration tests in `tests/test_export.py`. Fixed path typo
  `export_plan/` → `export_plans/` across planner/renderer/tests/
  README/CLI. Cleaned stale generated artifacts from
  `projects/demo-g8-triangle/` so the fixture stays in its expected
  pre-Stage-4b state. Full suite: 68 passed / 1 xfailed. Pushed to
  private remote as `aadb9a6`.

## Stage 3 MinerU adapter

- 2026-07-10 00:30 trae: added MinerU `pdf_info[]` schema adapter so
  Stage 3 runs end-to-end on real MinerU exports. New module
  `src/pdf2dt/bookview/mineru_adapter.py` flattens
  `pdf_info[].para_blocks[]` into the simplified `pages[].blocks[]`
  schema that the BookView builder already consumes.
  `_load_layout` now dispatches by schema (`pages[]` vs `pdf_info[]`)
  and no longer raises `BookViewBuildError` for real MinerU data.
  `_asset_refs_for_block` gained a third fallback that matches
  the asset registry `source_url` by hash prefix (MinerU emits a
  bare hash as `image_path` while Stage 2 records the full hash in
  `source_url`). `_make_book_item` Pass 1 now accepts `figure` blocks
  as primary so figure-only chapters (MinerU-VLM-on-scanned-PDF
  outputs) bind their assets. 4 new pytest cases; 62 passed /
  1 xfailed. End-to-end on `学之舟-总复习` produced 15 chapters /
  15 sections / 19 items / 13 asset refs.

## Stage 3 BookView builder

- 2026-07-09 16:05 trae: implemented Stage 3 BookView builder.
  New module `src/pdf2dt/bookview/{__init__,builder}.py` with
  `BookViewBuilder` linking items to layout blocks via two-pass
  scan (primary block + adjacent figure), binding localized assets
  via `assets_registry.json` (asset_id keyed, not URL), and
  inheriting topic_ids from Stage 4b. CLI gained `--book-view`.
  7 new pytest cases; 58 passed / 1 xfailed. Verified on synthetic
  fixture (20 items, 4 assets attached to their theorem/example
  items). On real MinerU data the builder raises a friendly
  `BookViewBuildError` because MinerU uses `pdf_info[]` rather
  than `pages[]`; tracked as the next task.

## Stage 4b outline matcher

- 2026-07-09 01:55 trae: implemented Stage 4b outline matching.
  New modules under `src/pdf2dt/outlining/` (`outline.py`,
  `items.py`, `matcher.py`) with public API
  `OutlineLoader / Outline / Topic / VocabularyEntry / Item /
  TopicAssignment / MatchReport / match_project`. Wired
  `--outline` into `scripts/run_pipeline.py`. 29 new pytest
  cases, 51 passed / 1 xfailed. Verified end-to-end on synthetic
  fixture (20 items) and on real `学之舟-总复习` (19 items,
  7 unclassified). The chicken-rabbit xfail was the seed of the
  negative-keyword veto contract delivered in v1.0.1.

## Git remote initialised

- 2026-07-09 01:35 trae: initialised local git repo and pushed to
  private remote `git@github.com:TyriontheimpLannister/math-content-preprocessor.git`
  via the user's existing SSH key. 47 files, 4485 insertions, only
  source + docs + outlines + schemas + tests. Confirmed `inbox/`,
  `projects/`, `inbox-sample/`, `_handoffs/`, `.uploads/` are
  excluded by `.gitignore`. HANDOFF and `Quick Index` updated.

## Outline review pass

- 2026-07-09 01:20 trae: applied user review decisions to
  `outlines/elementary-math-v1.yaml`. 专题七 merged from 8 节 to 4
  top-level blocks (comprehensive-explore / -word / -typical /
  -strategies); leaf count unchanged (113), all vocab keys remapped
  and re-checked. Mind-map routing kept on fallback path. Version
  bumped to 1.0.0.
- 2026-07-09 01:05 trae: drafted `outlines/elementary-math-v1.yaml`
  v0.1.0 — 113 leaf topics, 113 vocab entries, all ids slug-safe.
  Review draft at `projects/学之舟-总复习/outline_draft.md`.

## Public fork

- 2026-07-09 00:34 trae: user manually extracted
  `_tmp_pdf_to_deeptutor.7z` to `D:\AI_Data\Projects\pdf-to-deeptutor\`;
  tests pass there too (22 passed). Public fork is live on the user's
  physical disk but not yet pushed to any remote.
- 2026-07-09 00:15 trae: spun up public fork
  `D:\AI_Data\Projects\pdf-to-deeptutor\` for a future GitHub release.
  Renamed package to `pdf2dt`, rewrote docs/decisions/ in
  domain-neutral language, added LICENSE (MIT), README, CONTRIBUTING,
  .gitignore. 22 pytest cases pass under the new name. Private data
  (this project) stays here as the canonical real-world test bench.

## Project bootstrap

- 2026-07-08 23:58 trae: rewrote HANDOFF.md per the multi-agent
  protocol template; archived the previous design-rationale handoff
  under `docs/handoffs/` and split stable decisions into
  `docs/decisions/`.
- 2026-07-08 22:50 trae: ran pipeline end-to-end on real MinerU VLM
  export (`学之舟-总复习`); added `LocalFirstDownloader` and relative
  `images/` resolution.
- 2026-07-08 21:30 trae: implemented `scripts/init_inbox_meta.py`;
  first successful fixture run produced `projects/demo-g8-triangle/`.
- 2026-07-08 19:00 trae: bootstrapped inbox contract and MinerU output
  loader; stages 0-2 wired together.
